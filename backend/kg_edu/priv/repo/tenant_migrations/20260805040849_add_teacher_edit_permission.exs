defmodule KgEdu.Repo.TenantMigrations.AddTeacherEditPermission do
  @moduledoc """
  Updates resources based on their most recent snapshots.

  This file was autogenerated with `mix ash_postgres.generate_migrations`
  """

  use Ecto.Migration

  def up do
    alter table(:users, prefix: prefix()) do
      add :edit_enabled, :boolean, default: true
      add :edit_period_start, :date
      add :edit_period_end, :date
    end
  end

  def down do
    alter table(:users, prefix: prefix()) do
      remove :edit_period_end
      remove :edit_period_start
      remove :edit_enabled
    end
  end
end
