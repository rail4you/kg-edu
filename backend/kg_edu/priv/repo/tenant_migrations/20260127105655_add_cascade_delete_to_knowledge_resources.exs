defmodule KgEdu.Repo.TenantMigrations.AddCascadeDeleteToKnowledgeResources do
  @moduledoc """
  Updates resources based on their most recent snapshots.

  This file was autogenerated with `mix ash_postgres.generate_migrations`
  """

  use Ecto.Migration

  def up do
    drop constraint(:knowledge_resources, "knowledge_resources_parent_knowledge_resource_id_fkey")

    drop constraint(:knowledge_resources, "knowledge_resources_parent_unit_id_fkey")

    drop constraint(:knowledge_resources, "knowledge_resources_parent_subject_id_fkey")

    alter table(:knowledge_resources, prefix: prefix()) do
      modify :parent_subject_id,
             references(:knowledge_resources,
               column: :id,
               name: "knowledge_resources_parent_subject_id_fkey",
               type: :uuid,
               prefix: prefix(),
               on_delete: :delete_all
             )

      modify :parent_unit_id,
             references(:knowledge_resources,
               column: :id,
               name: "knowledge_resources_parent_unit_id_fkey",
               type: :uuid,
               prefix: prefix(),
               on_delete: :delete_all
             )

      modify :parent_knowledge_resource_id,
             references(:knowledge_resources,
               column: :id,
               name: "knowledge_resources_parent_knowledge_resource_id_fkey",
               type: :uuid,
               prefix: prefix(),
               on_delete: :delete_all
             )
    end
  end

  def down do
    drop constraint(:knowledge_resources, "knowledge_resources_parent_subject_id_fkey")

    drop constraint(:knowledge_resources, "knowledge_resources_parent_unit_id_fkey")

    drop constraint(:knowledge_resources, "knowledge_resources_parent_knowledge_resource_id_fkey")

    alter table(:knowledge_resources, prefix: prefix()) do
      modify :parent_knowledge_resource_id,
             references(:knowledge_resources,
               column: :id,
               name: "knowledge_resources_parent_knowledge_resource_id_fkey",
               type: :uuid,
               prefix: prefix()
             )

      modify :parent_unit_id,
             references(:knowledge_resources,
               column: :id,
               name: "knowledge_resources_parent_unit_id_fkey",
               type: :uuid,
               prefix: prefix()
             )

      modify :parent_subject_id,
             references(:knowledge_resources,
               column: :id,
               name: "knowledge_resources_parent_subject_id_fkey",
               type: :uuid,
               prefix: prefix()
             )
    end
  end
end
