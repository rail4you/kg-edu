defmodule KgEdu.Repo.Migrations.AddCoursePhoto do
  @moduledoc """
  Updates resources based on their most recent snapshots.

  This file was autogenerated with `mix ash_postgres.generate_migrations`
  """

  use Ecto.Migration

  def up do
    alter table(:courses) do
      add :image_url, :text
    end
  end

  def down do
    alter table(:courses) do
      remove :image_url
    end
  end
end
