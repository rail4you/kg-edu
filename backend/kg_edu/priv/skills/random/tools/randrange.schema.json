{
  "type": "object",
  "properties": {
    "start": {
      "type": "integer",
      "description": "Range start (inclusive)"
    },
    "stop": {
      "type": "integer",
      "description": "Range stop (exclusive)"
    },
    "step": {
      "type": "integer",
      "description": "Step size (default 1)"
    }
  },
  "required": ["start", "stop"]
}
