## SSL CA certificates for SMTP/HTTPS
## System ca-certificates are installed in the Docker image, use those instead
# export ERL_FLAGS="-ssl cacertfile $RELEASE_ROOT/lib/kg_edu-$RELEASE_VSN/priv/cacerts.pem $ERL_FLAGS"
